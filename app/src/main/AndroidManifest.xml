<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.portfolio.makeitportfolio.hanlight_aos">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/hanlight_logo"
        android:label="한빛"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="AllowBackup">
        <activity
            android:name=".ResetPasswrodActivity"
            android:exported="true" />
        <activity
            android:name=".SearchPasswordActivity"
            android:exported="true" />
        <activity
            android:name=".SearchIdActivity"
            android:exported="true" />
        <activity android:name=".SignupActivity2" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>